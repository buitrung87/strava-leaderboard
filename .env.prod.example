# Strava API Configuration
# Get these from https://www.strava.com/settings/api
STRAVA_CLIENT_ID=your_client_id_here
STRAVA_CLIENT_SECRET=your_client_secret_here
STRAVA_REDIRECT_URI=http://your-domain.com/auth/callback

# Session Secret
# Generate with: openssl rand -hex 32
SESSION_SECRET=your_random_secret_key_at_least_32_characters_long

# Node Environment
NODE_ENV=production

# Port (optional, defaults to 3000)
PORT=3000

# MongoDB URI (using docker-compose service name)
MONGODB_URI=mongodb://mongodb:27017/strava-leaderboard
